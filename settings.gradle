pluginManagement{
    repositories {
        mavenCentral()
        maven {
            url "https://plugins.gradle.org/m2/"
        }

    }
}

rootProject.name = 'lab2'
rootProject.projectDir.listFiles({it.name.endsWith('module')} as FileFilter).each {include it.name}
